import { test } from '@playwright/test';

test.describe.skip('placeholder', () => {
  test('placeholder test - remove when real E2E tests exist', () => {
    // Placeholder to prevent Playwright from erroring on empty test directory.
    // Will be replaced by real E2E tests in later modules.
  });
});
