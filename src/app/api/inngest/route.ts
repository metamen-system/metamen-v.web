import { NextResponse } from 'next/server';

// TODO M21: Configurar serve() de Inngest con todas las funciones.
export async function GET(request: Request) {
  void request;

  return NextResponse.json({ message: 'Inngest endpoint — Implementar en M21' });
}

export async function POST(request: Request) {
  void request;

  return NextResponse.json({ message: 'Inngest endpoint — Implementar en M21' });
}
